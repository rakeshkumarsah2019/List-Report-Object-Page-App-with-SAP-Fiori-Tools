/**
 * Adds the line item records to the app schema (for the list report or an object page section comprising a table)
 * @param appSchema - the app specific schema that shall get enhanced
 * @param lineItemKeys - list of all line item records
 * @param lineItemId ?
 */
export declare function addLineItemsType(appSchema: object, lineItemKeys: string[], lineItemId?: string): void;
