"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Adds the line item records to the app schema (for the list report or an object page section comprising a table)
 * @param appSchema - the app specific schema that shall get enhanced
 * @param lineItemKeys - list of all line item records
 * @param lineItemId ?
 */
function addLineItemsType(appSchema, lineItemKeys, lineItemId) {
    appSchema['definitions'][lineItemId || 'LineItems'] = {
        type: 'object',
        properties: {},
        additionalProperties: false
    };
    if (lineItemKeys) {
        for (const key of lineItemKeys) {
            appSchema['definitions'][lineItemId || 'LineItems']['properties'][key] = {
                $ref: '#/definitions/TableColumn'
            };
        }
    }
}
exports.addLineItemsType = addLineItemsType;
//# sourceMappingURL=utils.js.map