import { AppProvider, Controllers } from '../../../common/appProvider';
import { Files } from '../../../common';
import { Manifest } from '../../../../specification/v4';
export declare enum Constants {
    OptionalQuery = ":?query:",
    Component = "sap/fe/AppComponent",
    Namespace = "sap.fe.templates"
}
export declare class V4AppProvider extends AppProvider {
    constructor(manifest: Manifest, schemas: Files);
    /**
     * Public method which receives application controllers content and handles it
     * @param {Controllers} controllers Object which contains string content of controllers
     */
    handleControllers(controllers: Controllers): void;
}
