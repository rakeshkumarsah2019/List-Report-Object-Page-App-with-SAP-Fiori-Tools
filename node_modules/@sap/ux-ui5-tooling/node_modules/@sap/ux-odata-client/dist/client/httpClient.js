"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
// eslint-disable-next-line import/no-duplicates
const axios_1 = __importDefault(require("axios"));
const connection_1 = require("../connection");
async function newHttpClient({ system, credentials, log, existingConnection }) {
    const connection = existingConnection || (await connection_1.connect(system, credentials, log));
    const headers = {
        Cookie: connection.cookies.toString()
    };
    if (connection.xsrfToken) {
        headers['x-csrf-token'] = connection.xsrfToken;
    }
    const httpClient = axios_1.default.create({
        baseURL: new URL(system.service, system.url).toString(),
        headers,
        withCredentials: true
    });
    return { connection, httpClient };
}
exports.newHttpClient = newHttpClient;
//# sourceMappingURL=httpClient.js.map