import { ODataClient, ODataVersion, Catalog, Ui5AbapRepository } from '../client';
import { SystemConfig, Credentials, ServiceInfo } from '../config';
export interface SerializedSapSystem {
    name: string;
    url: string;
    client?: string;
    serviceKeys?: ServiceInfo;
    username?: string;
    password?: string;
}
export declare class SapSystem {
    private _name;
    readonly config: SystemConfig;
    private credentials;
    private connection;
    static fromSerialized(s: SerializedSapSystem): SapSystem;
    static idFrom(url: string, client?: string): string;
    constructor(name: string, config: SystemConfig, credentials?: Credentials);
    get name(): string;
    set name(newName: string);
    isScp(): boolean;
    /** Get a unique key to represent an SAP System */
    getId(): string;
    serialize(): SerializedSapSystem;
    getService(path: string): Promise<ODataClient>;
    getCatalog(odataVersion?: ODataVersion): Promise<Catalog>;
    getUi5Repository(log?: Console): Promise<Ui5AbapRepository>;
    setCredentials(credentials: Credentials): void;
}
