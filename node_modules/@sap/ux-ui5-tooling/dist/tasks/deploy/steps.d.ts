import { ApplicationConfig, SystemConfig, Credentials, Logger } from '@sap/ux-odata-client';
import { Workspace } from '../../utils/types';
/**
 * Configuration structure for deployment task
 */
export interface DeploymentConfig {
    app: ApplicationConfig;
    target: SystemConfig;
    credentials?: Credentials;
    exclude?: string[];
    index?: boolean;
    test?: boolean;
}
/**
 * Validates a deployment config. Throws an error is the configuration is invalid
 * @param config config that is to be validated
 */
export declare function validateConfig(config: DeploymentConfig): boolean;
/**
 * Create a zip file with all files in the virtual project workspace.
 * @param workspace UI5 project workspace
 * @param namespace UI5 project name
 * @param config Deployment configuration
 * @param log Logger
 */
export declare function createArchive(workspace: Workspace, namespace: string, config: DeploymentConfig, log: Logger): Promise<string>;
