import { TaskParameters } from '../../utils/types';
import { DeploymentConfig } from './steps';
declare const _default: ({ workspace, options }: TaskParameters<DeploymentConfig>) => Promise<void>;
/**
 * Implementation of the standard hook for UI5 tooling build tasks
 * @param params: standard UI5 task input consisting of workspace information and configuration options.
 */
export = _default;
