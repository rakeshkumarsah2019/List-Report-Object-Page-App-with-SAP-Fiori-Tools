export declare function parseArgs(args: string[], paramMap: object, defaults?: object): object;
/**
 * Returns the next available port starting from 8080 and stopping at 9010.
 * If no port is available then an error is thrown
 */
export declare function getAvailablePort(start?: number, stop?: number): Promise<number>;
