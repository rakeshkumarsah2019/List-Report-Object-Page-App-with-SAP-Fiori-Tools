export interface AddResult {
    added: boolean;
    postTask?: () => Promise<void>;
}
export declare type addHandler = (args: string[]) => Promise<AddResult>;
export declare enum Artifact {
    DeployConfig = "deploy-config",
    FlpConfig = "flp-config"
}
/**
 * add command handler
 * @param args CLI arguments
 */
export declare function add(args: string[]): Promise<void>;
