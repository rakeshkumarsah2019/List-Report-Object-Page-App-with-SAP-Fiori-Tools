import { AddResult } from './add';
/**
 * Supported parameters
 */
interface Parameters {
    base?: string;
    config?: string;
    package?: string;
    transport?: string;
    index?: boolean;
    force?: boolean;
}
export declare function addDeployConfig(args: string[]): Promise<AddResult>;
export declare function handleABAP(params: Parameters): Promise<AddResult>;
export {};
